#if defined _inc_header
	#undef _inc_header
#endif

#define MAX_CHARSELECT 				(5)
#define MAX_CHARSELECT_TEXTDRAW		(MAX_CHARSELECT * 5) // +1 if display server logo, +5 for New Character button

#define CHARACTER_TABLE "characters"